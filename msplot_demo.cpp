#include "msplot.h"

int main()
{
    // MSPlot::Figure fig(800, 600); // Create figure container

    // // Create 4 subplots vertically
    // fig.addSubplot(4, 1, 0); // Altitude
    // fig.plot(time_array, altitude_array, "Altitude (m)", "blue");
    // fig.plot(time_array, target_altitude_array, "Target Altitude", "red");

    // fig.addSubplot(4, 1, 1); // Velocity
    // fig.plot(time_array, velocity_array, "Velocity (m/s)", "red");

    // fig.addSubplot(4, 1, 2); // Thrust
    // fig.plot(time_array, thrust_array, "Thrust (N)", "green");

    // fig.addSubplot(4, 1, 3); // Acceleration
    // fig.plot(time_array, acceleration_array, "Acceleration (m/s²)", "purple");

    // fig.render();
    return 0;
}
